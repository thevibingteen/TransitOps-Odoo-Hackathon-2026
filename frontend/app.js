const app = document.querySelector('#app');
const nav = [['Dashboard','◫'],['Vehicles','▣'],['Drivers','♙'],['Trips','↗'],['Maintenance','⚙'],['Fuel','◒'],['Expenses','₹'],['Analytics','◈'],['AI Assistant','✦'],['Settings','⚙']];
const meta = { Dashboard:['Command centre','A real-time pulse of your fleet operations'], Vehicles:['Vehicle fleet','Assets, availability, and service health'], Drivers:['Driver network','Licence compliance and safety performance'], Trips:['Trip control','Dispatch safely with intelligent fleet checks'], Maintenance:['Maintenance command','Service schedules and repair work'], Fuel:['Fuel intelligence','Usage and cost-per-kilometre control'], Expenses:['Financial operations','Operational spending and cost visibility'], Analytics:['Fleet analytics','Performance trends that drive decisions'], 'AI Assistant':['TransitOps Copilot','Ask natural-language questions of your operations'], Settings:['Workspace settings','Configure your operations environment'] };
const icon = { trip:'↗',maintenance:'⚙',fuel:'◒',alert:'!' }; let page='Dashboard', state=null, chat=false, realtime=null; let session=JSON.parse(localStorage.getItem('transitops_session')||'null');
const money = n => `₹${Number(n||0).toLocaleString('en-IN')}`;
const esc = s => String(s ?? '').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function api(url, options) { const r = await fetch(url, { headers:{'Content-Type':'application/json'}, ...options }); const data = await r.json(); if (!r.ok) { const e = new Error(data.message || 'Request failed'); e.errors = data.errors; throw e; } return data; }
async function load() { state = await api('/api/dashboard'); if(!realtime){realtime=new EventSource('/api/events');realtime.addEventListener('fleet',()=>load())} shell(); }
function authScreen(){return `<section class="auth-screen"><aside class="auth-art"><div class="brand"><div class="brandmark">↗</div><span>TransitOps AI<small>SMART FLEET OS</small></span></div><div class="eyebrow">OPERATIONS, INTELLIGENTLY CONNECTED</div><h1>Know exactly what your fleet is doing.</h1><p>TransitOps turns dispatch, maintenance, risk, and cost data into decisions your team can act on in real time.</p><div class="auth-feature"><span class="pulse"></span> Live fleet events · secured role-based workspace</div></aside><main class="auth-form-wrap"><form class="auth-form" id="authForm" onsubmit="event.preventDefault();submitAuth()"><div class="brand"><div class="brandmark">↗</div><span>TransitOps AI</span></div><h2 id="authTitle">Welcome back</h2><p id="authLead">Sign in to your protected fleet workspace.</p><div id="nameField" class="field" style="display:none">Full name<input name="name" placeholder="e.g. Saumitra Kapoor"></div><label class="field">Work email<input name="email" type="email" autocomplete="email" placeholder="name@company.com"></label><label class="field">Password<input name="password" type="password" autocomplete="current-password" placeholder="At least 8 characters"></label><label id="confirmField" class="field" style="display:none">Confirm password<input name="confirmPassword" type="password" autocomplete="new-password" placeholder="Repeat your password"></label><div class="remember"><label><input name="remember" type="checkbox" checked> Remember me</label><span id="forgot">Forgot password?</span></div><div class="error" id="authError"></div><button class="btn primary" id="authSubmit">Sign in securely</button><div class="auth-switch"><span id="switchText">New to TransitOps?</span> <button type="button" onclick="toggleAuth()" id="switchAction">Create an account</button></div><div class="auth-demo">Demo account: <b>demo@transitops.ai</b><br>Password: <b>Transit@2026</b></div></form></main></section>`}
let signup=false;window.toggleAuth=()=>{signup=!signup;app.innerHTML=authScreen();document.querySelector('#authTitle').textContent=signup?'Create your workspace':'Welcome back';document.querySelector('#authLead').textContent=signup?'Start with secure, role-aware access.':'Sign in to your protected fleet workspace.';document.querySelector('#nameField').style.display=signup?'block':'none';document.querySelector('#confirmField').style.display=signup?'block':'none';document.querySelector('#authSubmit').textContent=signup?'Create secure account':'Sign in securely';document.querySelector('#switchText').textContent=signup?'Already have an account?':'New to TransitOps?';document.querySelector('#switchAction').textContent=signup?'Sign in':'Create an account'};
window.submitAuth=async()=>{const input=Object.fromEntries(new FormData(document.querySelector('#authForm')).entries());const error=document.querySelector('#authError');error.textContent='';try{const result=await api(signup?'/api/auth/register':'/api/auth/login',{method:'POST',body:JSON.stringify(input)});session=result.user;localStorage.setItem('transitops_session',JSON.stringify(session));await load()}catch(e){error.textContent=Object.values(e.errors||{}).join(' ')||e.message}};
function sidebar(){return `<aside class="sidebar"><div class="brand"><div class="brandmark">↗</div><span>TransitOps AI<small>SMART FLEET OS</small></span></div><div class="nav-label">OPERATIONS</div><nav class="nav">${nav.slice(0,9).map(([n,i])=>`<button class="${page===n?'active':''}" onclick="go('${n}')"><span class="ico">${i}</span><span>${n}</span></button>`).join('')}</nav><div class="nav-label">WORKSPACE</div><nav class="nav"><button class="${page==='Settings'?'active':''}" onclick="go('Settings')"><span class="ico">⚙</span><span>Settings</span></button></nav><div class="sidebar-foot"><span>FLEET HEALTH SCORE</span><strong>${state.metrics.health} / 100 · Excellent</strong><div class="progress"><i></i></div></div></aside>`}
function header(){let [title,sub]=meta[page];return `<header class="topbar"><div class="crumb"><h1>${title}</h1><p><span class="pulse"></span>${sub}</p></div><div class="top-actions"><button class="search" onclick="palette()">⌕ Search anything <kbd>CTRL K</kbd></button><button class="icon-btn" onclick="toast('You’re all caught up')">♢</button><div class="avatar">SK</div></div></header>`}
function badge(status){let c=status==='Available'||status==='Completed'||status==='Closed'?'available':status==='Maintenance'||status==='In Progress'?'maintenance':'trip';return `<span class="badge ${c}">${esc(status)}</span>`}
function hero(action='Create trip'){let [t,s]=meta[page];return `<div class="hero"><div><div class="eyebrow">LIVE OPERATIONAL DATA</div><h2>${t}</h2><p>${s}</p></div><button class="btn primary" onclick="${action==='Add vehicle'?'openVehicle()':'openTrip()'}">＋ ${action}</button></div>`}
function kpis(){let m=state.metrics; return `<div class="kpis"><div class="card kpi"><span>TOTAL VEHICLES</span><b>${m.vehicles}</b><span class="up">↑ 4.2% this month</span></div><div class="card kpi"><span>VEHICLES ON TRIP</span><b>${m.onTrip}</b><span class="up">Live dispatch status</span></div><div class="card kpi"><span>DRIVERS ON DUTY</span><b>${m.driversOnDuty}</b><span class="up">↑ 8 from yesterday</span></div><div class="card kpi"><span>TODAY'S EXPENSES</span><b>${money(m.expensesToday)}</b><span class="down">Fuel spend elevated</span></div></div>`}
function dashboard(){let h=[42,55,49,70,61,78,66,83,72,91,80,88], mon=['AUG','SEP','OCT','NOV','DEC','JAN','FEB','MAR','APR','MAY','JUN','JUL']; return `<section class="content">${hero()}${kpis()}<div class="dashboard-grid"><article class="card panel"><div class="panel-title"><h3>Trips & fuel performance</h3><span>Last 12 months ▾</span></div><div class="chart">${h.map((v,i)=>`<div class="bar-wrap"><i class="bar" style="height:${v}%"></i><i class="bar fuel" style="height:${Math.max(v-27,18)}%"></i><label>${mon[i]}</label></div>`).join('')}</div></article><article class="card panel"><div class="panel-title"><h3>Vehicle availability</h3><span><span class="pulse"></span>Live</span></div><div class="donut-row"><div class="donut"></div><div class="legend"><div><i class="dot" style="background:#c54eff"></i>Available <b>2</b></div><div><i class="dot" style="background:#5de2bd"></i>On trip <b>2</b></div><div><i class="dot" style="background:#f6b74d"></i>Maintenance <b>1</b></div><div><i class="dot" style="background:#663a7d"></i>Retired <b>0</b></div></div></div></article></div><div class="lower"><article class="card panel"><div class="panel-title"><h3>Operational activity</h3><span>Live feed</span></div>${state.activities.slice(0,4).map(a=>`<div class="activity"><div class="activity-icon">${icon[a.type]||'•'}</div><div><b>${esc(a.title)}</b><small>${esc(a.detail)} · ${a.at}</small></div></div>`).join('')}</article><article class="card panel insight"><h3>✦ AI operational signal</h3><p>Fuel spend is trending 12% above your baseline on the Western route. Two routes have low-load overlap.</p><div class="insight-line">Estimated monthly saving <b>${money(50000)}</b></div><button class="btn" onclick="go('AI Assistant')">Explore recommendation →</button></article></div>${tableCard(state.trips,'Active trips','Trips')}</section>`}
function tableCard(items,title,view){return `<article class="card data-card"><header><h3>${title}</h3><button class="btn" onclick="go('${view}')">View all →</button></header><table class="data-table"><thead><tr><th>REFERENCE</th><th>ROUTE / ASSET</th><th>ASSIGNED</th><th>STATUS</th><th>ETA / HEALTH</th></tr></thead><tbody>${items.slice(0,7).map(r=>`<tr><td><b>${esc(r.id||r.registration)}</b></td><td>${esc(r.source ? `${r.source} → ${r.destination}` : `${r.name} · ${r.type}`)}</td><td>${esc(r.driver||r.registration||'—')}</td><td>${badge(r.status)}</td><td>${esc(r.eta||`${r.health||r.safety||'—'}%`)}</td></tr>`).join('')}</tbody></table></article>`}
async function records(kind){let res = await api(`/api/${kind}`); let map={vehicles:['Vehicle fleet','Vehicles','Add vehicle'],drivers:['Driver network','Drivers','Create trip'],trips:['Trip control','Trips','Create trip'],maintenance:['Maintenance command','Maintenance','Create trip'],expenses:['Financial operations','Expenses','Create trip']}[kind]; let items=res.items; return `<section class="content">${hero(map[2])}<div class="page-grid"><div>${tableCard(items,`${map[0]} records`,map[1])}</div><aside class="side-stack"><article class="card panel insight"><h3>✦ Smart insight</h3><p>${kind==='drivers'?'Two driver licences need attention this month.':'AI has identified fleet actions that could improve operational resilience.'}</p><button class="btn" onclick="go('AI Assistant')">Ask the copilot →</button></article><article class="card panel"><div class="panel-title"><h3>Data protection</h3><span>Secure</span></div><p style="font-size:11px;color:var(--muted);line-height:1.6">Validated API requests, predictable business rules, and relational schema design make every record auditable.</p></article></aside></div></section>`}
function analytics(){return `<section class="content">${hero('Create trip')}<div class="dashboard-grid"><article class="card panel"><div class="panel-title"><h3>Revenue & operating cost</h3><span>FY 2026</span></div><div class="chart">${[34,52,63,51,82,66,79,92,73,88,79,96].map((v,i)=>`<div class="bar-wrap"><i class="bar" style="height:${v}%"></i><i class="bar fuel" style="height:${v-20}%"></i><label>${['A','S','O','N','D','J','F','M','A','M','J','J'][i]}</label></div>`).join('')}</div></article><article class="card panel"><div class="panel-title"><h3>Fleet KPI health</h3><span>Updated now</span></div><div class="empty"><div class="orb">88</div><h3>Excellent performance</h3><p>Utilisation and safety score have improved over the past 30 days.</p></div></article></div><div class="lower"><article class="card panel"><div class="panel-title"><h3>Top performing vehicles</h3><span>ROI</span></div><div class="activity"><div class="activity-icon">1</div><div><b>TRK-114 · Zenith Cargo</b><small>₹1.24L revenue · 8.3 km/L</small></div></div><div class="activity"><div class="activity-icon">2</div><div><b>VAN-021 · Swift Line</b><small>₹92K revenue · 11.2 km/L</small></div></div></article><article class="card panel insight"><h3>Ready to export</h3><p>Use the secure reporting API for scheduled CSV and PDF export workflows.</p><button class="btn" onclick="toast('Report export queued')">Queue export</button></article></div></section>`}
function assistant(){return `<section class="content"><div class="empty card"><div class="orb">✦</div><h3>Ask the fleet copilot</h3><p>Get an evidence-based dispatch recommendation, identify fuel anomalies, or find documents at risk of expiry.</p><br><button class="btn primary" onclick="toggleChat()">Open TransitOps AI</button></div></section>`}
function settings(){return `<section class="content"><div class="page-grid"><article class="card panel"><div class="panel-title"><h3>Workspace access</h3><span>Role-based</span></div><div class="activity"><div class="activity-icon">SK</div><div><b>Saumitra Kapoor · Fleet Manager</b><small>Full operational access</small></div></div><div class="activity"><div class="activity-icon">DN</div><div><b>Dispatch team</b><small>Trip assignment and status updates</small></div></div><div class="activity"><div class="activity-icon">SO</div><div><b>Safety officers</b><small>Compliance and maintenance only</small></div></div></article><article class="card panel insight"><h3>Security posture</h3><p>Input validation is enforced in both the browser and API layer. Production schema uses foreign keys, checks, indexes, and unique constraints.</p></article></div></section>`}
async function content(){if(!state)return `<section class="content"><div class="empty"><div class="orb">↗</div><p>Loading real-time operations…</p></div></section>`;if(page==='Dashboard')return dashboard();if(page==='Analytics')return analytics();if(page==='AI Assistant')return assistant();if(page==='Settings')return settings();if(page==='Fuel')return records('expenses');return records(page.toLowerCase());}
async function shell(){if(!session){app.innerHTML=authScreen();return}app.innerHTML=`${sidebar()}<main class="main">${header()}<div id="view"><section class="content"><div class="empty"><div class="orb">↗</div><p>Loading data…</p></div></section></div></main>${chat?chatBox():`<button class="chat-toggle" onclick="toggleChat()">✦</button>`}`;document.querySelector('#view').innerHTML=await content();}
function chatBox(){return `<div class="chat"><div class="chat-head">✦ TransitOps Copilot <button style="float:right;background:none;border:0;color:white;cursor:pointer" onclick="toggleChat()">×</button></div><div class="chat-body" id="chatBody"><b style="color:white">Your operational intelligence layer</b><br>Ask: “Which vehicles are available?” or “Who has an expiring licence?”</div><div class="chat-foot"><input id="ask" placeholder="Ask about your fleet…" onkeydown="if(event.key==='Enter')askAI()"><button class="btn primary" onclick="askAI()">Ask</button></div></div>`}
window.go=async n=>{page=n;await shell()}; window.toggleChat=async()=>{chat=!chat;await shell()}; window.askAI=async()=>{let q=document.querySelector('#ask').value;let box=document.querySelector('#chatBody'); box.textContent='Thinking with live fleet data…';try{let r=await api('/api/ai/chat',{method:'POST',body:JSON.stringify({question:q})});box.innerHTML=`<b style="color:#fff">AI response</b><br>${esc(r.answer)}`}catch(e){box.innerHTML=`<b>Need a little more detail</b><br>${esc(e.errors?.question||e.message)}`}};
window.openTrip=()=>{const opts=state.vehicles.filter(v=>v.status==='Available').map(v=>`<option value="${v.id}">${esc(v.registration)} · ${esc(v.name)}</option>`).join('');const d=state.drivers.filter(x=>x.status==='Available').map(x=>`<option value="${x.id}">${esc(x.name)} · safety ${x.safety}</option>`).join('');modal('Create a dispatch trip',`<p>Dispatch checks will verify availability, licence validity, and load capacity before creating the trip.</p><div class="form-grid"><label class="field">Source<input name="source" placeholder="e.g. Mumbai"></label><label class="field">Destination<input name="destination" placeholder="e.g. Pune"></label><label class="field">Available vehicle<select name="vehicleId"><option value="">Choose vehicle</option>${opts}</select></label><label class="field">Available driver<select name="driverId"><option value="">Choose driver</option>${d}</select></label><label class="field">Cargo weight (kg)<input name="cargoWeight" type="number" min="1" placeholder="e.g. 4800"></label><label class="field">Distance (km)<input name="distance" type="number" min="1" placeholder="e.g. 149"></label><label class="field">Estimated revenue (₹)<input name="revenue" type="number" min="1" placeholder="e.g. 65000"></label></div>`,`Create & dispatch`,`submitTrip()`)};
window.openVehicle=()=>modal('Add a vehicle',`<p>Vehicle registrations are validated and saved as persistent operational records.</p><div class="form-grid"><label class="field">Registration number<input name="registration" placeholder="e.g. MH 12 AB 1234"></label><label class="field">Vehicle name<input name="name" placeholder="e.g. Atlas Cargo"></label><label class="field">Vehicle type<select name="type"><option value="">Choose type</option><option>Truck</option><option>Van</option><option>Trailer</option><option>Bus</option></select></label><label class="field">Capacity (kg)<input name="capacity" type="number" min="1"></label><label class="field">Fuel type<select name="fuel"><option>Diesel</option><option>Petrol</option><option>Electric</option></select></label><label class="field">Current odometer<input name="odometer" type="number" min="0"></label></div>`,`Save vehicle`,`submitVehicle()`);
function modal(title,body,action,call){document.body.insertAdjacentHTML('beforeend',`<div class="modal" id="modal"><form class="modal-box" id="form" onsubmit="event.preventDefault();${call}"><h2>${title}</h2>${body}<div class="error" id="formError"></div><div class="modal-actions"><button type="button" class="btn" onclick="closeModal()">Cancel</button><button class="btn primary">${action}</button></div></form></div>`)} window.closeModal=()=>document.querySelector('#modal')?.remove();
function fields(){return Object.fromEntries(new FormData(document.querySelector('#form')).entries())} function setErrors(err){document.querySelector('#formError').textContent=Object.values(err||{}).join(' ')||'Please review the entered values.'}
window.submitTrip=async()=>{try{let r=await api('/api/trips',{method:'POST',body:JSON.stringify(fields())});closeModal();toast(`${r.item.id} dispatched with AI recommendation`);await load()}catch(e){setErrors(e.errors)}};window.submitVehicle=async()=>{try{let r=await api('/api/vehicles',{method:'POST',body:JSON.stringify(fields())});closeModal();toast(`${r.item.registration} added to your fleet`);await load()}catch(e){setErrors(e.errors)}};
window.palette=()=>{document.body.insertAdjacentHTML('beforeend',`<div class="palette" id="palette"><input autofocus placeholder="Search vehicles, drivers, trips…" onkeydown="if(event.key==='Escape')closePalette()"><div class="command" onclick="go('Vehicles');closePalette()">▣ &nbsp; Browse vehicle fleet</div><div class="command" onclick="openTrip();closePalette()">↗ &nbsp; Create dispatch trip</div><div class="command" onclick="go('AI Assistant');closePalette()">✦ &nbsp; Ask TransitOps Copilot</div></div>`)};window.closePalette=()=>document.querySelector('#palette')?.remove();window.toast=m=>{document.body.insertAdjacentHTML('beforeend',`<div class="toast">✓ ${esc(m)}</div>`);setTimeout(()=>document.querySelector('.toast')?.remove(),3000)};document.addEventListener('keydown',e=>{if(e.ctrlKey&&e.key.toLowerCase()==='k'){e.preventDefault();if(!document.querySelector('#palette'))palette()}if(e.key==='Escape')closePalette()});load().catch(e=>{app.innerHTML=`<div class="empty"><div class="orb">!</div><h3>Could not connect to TransitOps API</h3><p>${esc(e.message)}</p></div>`});
